image: mcr.microsoft.com/dotnet/core/sdk:3.1

stages:
    - build
    
build:
    stage: build
    
    script:
        - "cd ApiBase"
        - "dotnet build"

test:
    stage: test

    script:
    - "cd ApiBase.IntegrationTests"
    - "dotnet test"