image: mcr.microsoft.com/dotnet/core/sdk:3.1

stages:
  - core
  - data
  - test

core:
  stage: core

  script:
    - "cd Core/ApiBase"
    - "dotnet build"

data:
  stage: data

  script:
    - "cd Data/AirQi"
    - "dotnet build"

test:
  stage: test

  script:
    - "cd Core"
    - "dotnet test"
