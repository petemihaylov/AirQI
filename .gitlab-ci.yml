image: mcr.microsoft.com/dotnet/core/sdk:3.1

stages:
    - build
    - test
    
build:
    stage: build

    script:
        - "cd ApiBase"
        - "dotnet build"
test:
    stage: test

    script:
        - "dotnet test"